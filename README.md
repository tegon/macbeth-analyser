# macbeth-analyser

## Setup

Clone the repository, then install the dependencies:

```bash
cd macbeth-analyser && bundle install
```

## Test suite

Run the tests with:

```bash
rake test
```

## Runnning

Run the analyser:

```bash
ruby analyser.rb
```
